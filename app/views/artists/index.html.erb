<p id="notice"><%= notice %></p>

<h1>Artists</h1>

<table class="table">
  <thead>
    <tr>
      <th scope="col">Name</th>
      <th scope="col">Year</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>
  <tbody id="artists">
    <%= render @artists %>
  </tbody>
</table>

<br>
<% if current_user %>
  <h2>Create Artist</h2>
  <%= render 'form', artist: @artist %>
<% end %>
